---
phase: 01-error-handling-coverage
plan: 02
type: execute
---

<objective>
Add error handling tests for OQL validation logic and API handler error responses.

Purpose: Increase OQL package and handler test coverage by testing validation edge cases and HTTP error response paths.
Output: Enhanced translator_test.go and oql_integration_test.go with validation and API error coverage.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/oql-test-report.md
@.planning/codebase/TESTING.md
@api/internal/services/query/oql/translator.go
@api/internal/services/query/oql/translator_test.go
@api/internal/handlers/query.go
@api/internal/handlers/oql_integration_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add translator validation error tests to translator_test.go</name>
  <files>api/internal/services/query/oql/translator_test.go</files>
  <action>Add table-driven tests for translator validation and edge case scenarios. Test cases should include:
- Invalid dot-walking depth: "a.b.c.d.e.f" (exceeds reasonable depth)
- Invalid field references: "nonexistent.field = true"
- Mixed entity dot-walking: "asset.findings.severity = 'critical'" (invalid cross-entity)
- Invalid operator for field type: string field with numeric operator
- Empty filter list (valid but test translation)
- Nested filters exceeding depth: AND(OR(AND(OR(...)))) (excessive nesting)
- Invalid limit values: limit -1, limit 0, limit 1000000 (should validate/clamp)
- Invalid sort direction: "field sort invalid_direction"
- Multiple sorts (if not supported, test error)
- Sort on non-existent field: "nonexistent_field sort asc"

Follow existing translator_test.go patterns. Use table-driven tests with t.Run(). Verify:
1. Translation returns appropriate error or result
2. Error messages are descriptive
3. Valid edge cases translate correctly
4. Invalid cases return proper errors</action>
  <verify>go test ./internal/services/query/oql -run TestTranslateError -v passes with all validation tests</verify>
  <done>translator_test.go has 10+ new validation/edge case tests, translator coverage increased</done>
</task>

<task type="auto">
  <name>Task 2: Add API handler error response tests to oql_integration_test.go</name>
  <files>api/internal/handlers/oql_integration_test.go</files>
  <action>Add error response tests for OQL API endpoints (QueryOQL, ValidateOQL, ExplainOQL). Test cases should include:

For QueryOQL handler:
- Malformed JSON request body
- Missing "query" field in request
- Empty query string
- Invalid OQL syntax (returns 400 with error details)
- Invalid tenant context (returns 401/403)
- Query executor errors (mock executor returning errors)

For ValidateOQL handler:
- Valid OQL (returns 200 with validation result)
- Invalid OQL syntax (returns 400 with error details)
- Empty query (returns 400)
- Malformed JSON

For ExplainOQL handler:
- Valid OQL (returns 200 with SQL translation)
- Invalid OQL syntax (returns 400 with error details)
- Empty query (returns 400)
- Malformed JSON

Use existing oql_integration_test.go patterns. Verify:
1. Correct HTTP status codes (400 for bad requests, 401/403 for auth)
2. Error response structure matches API spec
3. Error messages are descriptive
4. Valid requests return 200 with expected response structure</action>
  <verify>go test ./internal/handlers -run TestOQL.*Error -v passes with all error response tests</verify>
  <done>oql_integration_test.go has 12+ new API error response tests, handler error paths covered</done>
</task>

<task type="auto">
  <name>Task 3: Run coverage report and verify validation/handler improvement</name>
  <files></files>
  <action>Run coverage report to verify validation and handler tests increased coverage:
```bash
go test ./internal/services/query/oql -coverprofile=oql_coverage.out
go tool cover -func=oql_coverage.out | grep translator.go

go test ./internal/handlers -coverprofile=handlers_coverage.out
go tool cover -func=handlers_coverage.out | grep query.go
```
Target: translator.go should show ≥80% coverage, query.go (handlers) should show increased coverage from baseline 32.7%. If not, identify remaining uncovered lines and add targeted tests.</action>
  <verify>translator.go coverage ≥80%, query.go coverage increased from 32.7% baseline</verify>
  <done>Translator and handler error paths comprehensively tested, coverage targets met</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./internal/services/query/oql -v` passes with all new tests
- [ ] `go test ./internal/handlers -run TestOQL.*Error -v` passes with all handler error tests
- [ ] `go test ./internal/services/query/oql -cover` shows increased coverage
- [ ] No test failures or skipped tests
- [ ] Error response structures match API specification
</verification>

<success_criteria>

- Translator validation error paths tested (10+ new scenarios)
- API handler error responses tested for all 3 OQL endpoints (12+ tests)
- translator.go coverage ≥80%
- query.go handler coverage increased from baseline
- All error responses return correct HTTP status codes and structures
  </success_criteria>

<output>
After completion, create `.planning/phases/01-error-handling-coverage/01-02-SUMMARY.md`:

# Plan 01-02: Validation and API Handler Error Tests Summary

**Added 22+ error handling tests for OQL validation logic and HTTP handler error responses**

## Accomplishments

- Added translator validation tests for dot-walking, field references, operators, limits, sorts
- Added API handler error response tests for QueryOQL, ValidateOQL, ExplainOQL endpoints
- Increased translator.go coverage to ≥80%
- Increased handler coverage from baseline 32.7%
- Verified all error responses return correct HTTP status codes

## Files Created/Modified

- `api/internal/services/query/oql/translator_test.go` - Added 10+ validation/edge case tests
- `api/internal/handlers/oql_integration_test.go` - Added 12+ API error response tests

## Decisions Made

None - followed existing test patterns

## Issues Encountered

None

## Next Step

Ready for 01-03-PLAN.md (Coverage Verification and Final Report)
</output>
