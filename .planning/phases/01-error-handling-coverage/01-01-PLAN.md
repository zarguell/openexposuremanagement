---
phase: 01-error-handling-coverage
plan: 01
type: execute
---

<objective>
Add error handling tests for OQL parser and tokenizer error paths.

Purpose: Increase OQL package test coverage from 74.6% to 80%+ by testing untested error scenarios in core parsing components.
Output: Enhanced parser_test.go and tokenizer_test.go with comprehensive error path coverage.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/oql-test-report.md
@.planning/codebase/TESTING.md
@api/internal/services/query/oql/parser.go
@api/internal/services/query/oql/tokenizer.go
@api/internal/services/query/oql/parser_test.go
@api/internal/services/query/oql/tokenizer_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parser error path tests to parser_test.go</name>
  <files>api/internal/services/query/oql/parser_test.go</files>
  <action>Add table-driven tests for untested parser error scenarios. Test cases should include:
- Unclosed parentheses: "is_active = true AND (software.vendor = 'Microsoft'"
- Mismatched parentheses: "is_active = true AND (software.vendor = 'Microsoft'))"
- Invalid operator combinations: "is_active = = true", "is_active > > true"
- Missing operands: "is_active =", "= true", "AND true"
- Invalid field names with special characters: "is@active = true"
- Unclosed string literals: "is_active = 'true"
- Invalid escape sequences: "is_active = '\\'"
- Empty parentheses: "()"
- Multiple operators without operands: "is_active AND OR false"
- Invalid NOT usage: "NOT NOT true", "NOT (true AND)"

Use table-driven test pattern from existing parser_test.go. Each test should verify:
1. Parser returns non-nil error
2. Error message is descriptive
3. Error position is accurate (if available)</action>
  <verify>go test ./internal/services/query/oql -run TestParseError -v passes with all new error tests</verify>
  <done>parser_test.go has 10+ new error scenarios tested, all parser error paths covered</done>
</task>

<task type="auto">
  <name>Task 2: Add tokenizer error path tests to tokenizer_test.go</name>
  <files>api/internal/services/query/oql/tokenizer_test.go</files>
  <action>Add table-driven tests for untested tokenizer error scenarios. Test cases should include:
- Invalid characters in identifiers: "@#$ = true"
- Malformed numbers: "123.456.789", "1.2.3", "0x123"
- Invalid string literals: "is_active = 'unclosed"
- Mixed quotes: `is_active = "mixed'`
- Unterminated escapes: `is_active = '\\`
- Unicode edge cases: "is_active = '\u'"
- Invalid operators: "===", "!==", "<<", ">>"
- Whitespace edge cases: leading/trailing whitespace (should be valid but test token positions)
- Tab characters in input (should be valid, test positions)

Follow existing tokenizer_test.go patterns. Use t.Run() for sub-tests. Verify:
1. Tokenizer returns appropriate error or handles gracefully
2. For valid edge cases, tokens are correct
3. Position tracking is accurate</action>
  <verify>go test ./internal/services/query/oql -run TestTokenizeError -v passes with all new tokenizer tests</verify>
  <done>tokenizer_test.go has 8+ new error/edge case tests, all tokenizer paths covered</done>
</task>

<task type="auto">
  <name>Task 3: Run coverage report and verify error path improvement</name>
  <files></files>
  <action>Run coverage report to verify error path tests increased coverage:
```bash
go test ./internal/services/query/oql -coverprofile=coverage.out
go tool cover -func=coverage.out | grep -E "(parser|tokenizer)\.go"
```
Target: parser.go and tokenizer.go should show ≥80% coverage. If not, identify remaining uncovered lines and add targeted tests for those specific error paths.</action>
  <verify>Coverage report shows parser.go and tokenizer.go each ≥80%</verify>
  <done>Parser and tokenizer coverage at 80%+, error paths comprehensively tested</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./internal/services/query/oql -v` passes with all new tests
- [ ] `go test ./internal/services/query/oql -cover` shows coverage increased
- [ ] No test failures or skipped tests
- [ ] Code follows existing test patterns (table-driven, t.Run for sub-tests)
</verification>

<success_criteria>

- Parser error paths comprehensively tested (10+ new error scenarios)
- Tokenizer error paths comprehensively tested (8+ new edge cases)
- Coverage increased for parser.go and tokenizer.go (target ≥80%)
- All new tests pass
- Tests follow Go testing conventions (table-driven, descriptive names)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-error-handling-coverage/01-01-SUMMARY.md`:

# Plan 01-01: Parser and Tokenizer Error Paths Summary

**Added 18+ error handling tests for OQL parser and tokenizer components**

## Accomplishments

- Added parser error tests for unclosed/mismatched parentheses, invalid operators, missing operands
- Added tokenizer edge case tests for malformed numbers, invalid strings, special characters
- Increased parser.go and tokenizer.go coverage to ≥80%
- All tests follow existing table-driven patterns

## Files Created/Modified

- `api/internal/services/query/oql/parser_test.go` - Added 10+ parser error scenario tests
- `api/internal/services/query/oql/tokenizer_test.go` - Added 8+ tokenizer edge case tests

## Decisions Made

None - followed existing test patterns

## Issues Encountered

None

## Next Step

Ready for 01-02-PLAN.md (Validation and API Handler Error Tests)
</output>
