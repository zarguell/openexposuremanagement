import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>
      <h1>Open Exposure Management</h1>
      <p>UI placeholder - implementation pending</p>
    </div>
  </React.StrictMode>,
)
